# 用于处理obsidian到hugo的文件格式转换

## 第一步：使用obsidian_to_hugo的python包

### install

```sh
pip install obsidian-to-hugo
```

### usage

```sh
# example
python -m obsidian_to_hugo --hugo-content-dir ~/Documents/qytlix.github.io/content --obsidian-vault-dir ~/Documents/Obsidian/Fiction

python -m obsidian_to_hugo --hugo-content-dir ~/Documents/qytlix.github.io/content/tech --obsidian-vault-dir ~/Documents/Obsidian/Diary
```

注意：对于代码块中的方括号，这个还处理得不是很好，如果报错需要手动更新。

## 第二步：使用脚本处理yaml元数据

script generated by deepseek 20260408

usage:

```sh
python add_frontmatter.py content
```

递归地处理所有文件的元数据。

## 第三步：处理tags和categories

在obsidian中，可以使用`#tag`来使用标签。

以路径作为集合。

script generated by deepseek 20260408

usage:

```sh
python add_categories_tags.py content
```

注意：需要`pyyaml`

## 第四步：移动image到static

```sh
python scripts/move_images.py ~/Documents/Obsidian/Fiction ~/Documents/qytlix.github.io/content --image-prefix /x/images
```
